extends ../templates/default

block infopane
  div#sidebar

block contentpane
  div#map
    leaflet(id="map" lf-center="quebec" layers="layers" controls="controls" markers="markers" defaults="defaults")
