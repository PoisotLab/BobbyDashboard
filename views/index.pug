html
    head
        script(type="stylesheet" src="https://unpkg.com/leaflet@1.0.3/dist/leaflet.css")
        script(src="https://unpkg.com/leaflet@1.0.3/dist/leaflet.js")
        title Test
    body
        div#map(style={height: "180px"})
        script.
            var mymap = L.map('map').setView([51.505, -0.09], 13);

